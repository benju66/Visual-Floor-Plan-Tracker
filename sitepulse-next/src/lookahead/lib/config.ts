import type { Accent } from "./types";

// Prototype "props" that aren't part of saved settings.
// Converged to SitePulse's blue (`getAccent` Blue = #2563eb) — the absorbed
// default was Orange. The Orange/Slate branches in `getAccent` are left intact.
export const ACCENT: Accent = "Blue";
export const SHOW_NOTES = true;
