[pytest]
# Only collect the dedicated tests/ package. This deliberately excludes the
# ad-hoc test_fitz.py scratch script at the repo root.
testpaths = tests
python_files = test_*.py
python_functions = test_*
